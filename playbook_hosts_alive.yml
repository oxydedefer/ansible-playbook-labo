---

- hosts: toolsserverlabo
  gather_facts: no
  become: yes
  vars:
    ansible_ssh_private_key_file: /home/.ssh/id_rsa_{{ansible_user}}
  tasks:

    - ping:

    - debug:
        msg: "{{ variable_in_vault }}"

  vars_files:
    - "{{vault_file_path}}"
